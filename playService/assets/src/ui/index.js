var h = require('snabbdom/h').default
/*var editButton = require('./edit-button')
var footer = require('./footer')
var tiles = require('./tiles')*/

function render(model, update) {
  return h('h1', 'Game List:')
}

module.exports = render
